package cn.pxl.capture04.common;

//测试包访问规则
//类被定义为缺省，只能被当前包下所有的类使用
class PackageEntity {

    //当前类中标记为public的部分(类，方法，变量)，可以被所有包中的所有类使用。
    //当前类中标记为private的部分（类，方法，变量），只能被当前类使用；
    //当前类中没有标记的部分（缺省），可以被当前类所在的包下所有的类使用；




}
